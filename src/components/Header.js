//We are importing React library in every file because this library is very important
import React from "react";

//A component is practically a function that returns something
function Header() {
  return (
    <header>
      <h1>Keeper App</h1>
    </header>
  );
}

//EXPORT IS IMPORTANT!!
export default Header;
